## Create Test vs Control mapping using K Nearest Neighbour

This code uses multiple parameters at a user_id level and finds its closest user_id who doesn't fall into a particular group.

While doing any analysis, instead of comparing the customers in Test group vs everybody, this method can be employed to find similar customers, and their metrics can be compared
